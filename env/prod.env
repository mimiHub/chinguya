# 운영 환경 — 프론트와 Core API가 같은 EC2에 있으므로 internal-IP로 붙는다.
# 이 주소는 Next 서버(Route Handler)만 사용한다. 브라우저에는 절대 노출되지 않는다
# (NEXT_PUBLIC_ 접두사를 붙이지 말 것 — 붙이면 클라이언트 번들에 박혀 동작하지 않는다).
#
APP_ENV=prod
CORE_API_BASE_URL=http://localhost:8080
